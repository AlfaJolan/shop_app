Да, именно так 👍 Мы теперь базу выровняли → можно идти по слоям сверху.

---

## 🔹 План следующего шага

1. **Публичная часть (`invoice.html` + `routers/invoice.py`)**

   * На странице накладной у покупателя добавить форму для загрузки чеков (PDF).
   * В роутере `invoice.py` сделать эндпоинт `/invoice/{id}/receipts/upload`, который:

     * принимает `UploadFile` (один или несколько),
     * кладёт в `static/uploads/receipts/{invoice_id}/...`,
     * пишет запись в `invoice_receipts` (`status = pending`, `amount = NULL`, `expired_at = now+2d`).

2. **Админка (`admin_invoice.py` + `templates/admin/invoice_edit.html`)**

   * На странице накладной админ видит список чеков.
   * Для каждого чека:

     * кнопка **✅ Подтвердить** → `status=approved`, записываем `amount`.
     * кнопка **❌ Отклонить** → `status=rejected`.
   * Если хотя бы один чек `approved` → ставим `invoice.is_paid = True`, `status = paid`.

3. **Уведомления**

   * В админских роутерах при подтверждении чека → дергаем функцию, которая отправит сообщение в Telegram.

4. **Очистка**

   * Добавляем задачу (APScheduler) для очистки файлов старше 2 дней с `status=pending`, ставим им `status=expired`.

---

👉 Предлагаю начать с **шаг 1**: сделать роут загрузки чеков (`upload_receipt`) и форму в `invoice.html`.
Хочешь, я дам тебе сразу полный код роута + шаблона, чтобы вставил и протестил загрузку файлов?
